export const BLOCKS: number = 21;
export let currentBlock: number = 892341;
export let targetBlock: number = currentBlock + BLOCKS - 1;

export const state = {
    selectedNumber: null as number | null
};
